import google from "../pages/google/google.js"

describe('template spec', () => {
  it('passes', () => {
    cy.visit('')
    cy.wait(1000)
    // cy.get(google.field_input).type("Hello World")
  })
})